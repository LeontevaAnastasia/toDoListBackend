spring:
  config:
    activate:
      on-profile: ide
  redis:
    host: localhost
    port: 6379

  rabbitmq:
    host: localhost
    port: 5672
  datasource:
    url: jdbc:postgresql://localhost:5444/toDoList-api
    username: admin
    password: admin
